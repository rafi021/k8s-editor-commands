apiVersion: v1
kind: Secret
metadata:
  name: laravel-secret
  labels:
    app: laravel
    component: secret
# NOTE: All values must be base64 encoded. Replace PLACEHOLDER values accordingly.
# Example: echo -n 'your-app-key' | base64
# For Laravel APP_KEY (starting with base64:...) encode full string including base64: prefix.
# DB_PASSWORD example: echo -n 'StrongPass123' | base64

type: Opaque
data:
  APP_KEY: "UEhBQ0VIT0xERVJfQVBQX0tFWQ=="            # Replace with base64 encoded APP_KEY
  DB_USERNAME: "bGFyYXZlbA=="                      # base64(laravel)
  DB_PASSWORD: "U1RST05HX1BBU1NXT1JE"              # Replace with base64 encoded password
  DB_ROOT_PASSWORD: "Uk9PVF9QQVNTV09SRA=="        # Replace with base64 encoded MySQL root password
  REDIS_PASSWORD: ""                               # Leave empty or add if Redis auth enabled